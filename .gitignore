node_modules/
docs/.vuepress/.cache/
docs/.vuepress/.temp/
docs/.vuepress/dist/
.DS_Store
*.log

# VuePress 加载配置时生成的临时 bundle（正常构建结束时自动删除，
# 构建被中断时会残留）
docs/.vuepress/config.ts.*.mjs

# 本地环境变量（存放娱乐场访问密码等敏感信息，切勿提交）
.env
.env.local
.env.*.local

# 协作工具本地数据（工作记录等）
.workbuddy/
