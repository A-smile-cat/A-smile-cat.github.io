# 复制本文件为 .env 并填入真实密码（.env 已被 .gitignore 忽略，不会进入仓库）
#
# 娱乐场 /entertainment/ 访问密码。构建时由 theme-hope 做 bcrypt 哈希后写入产物，
# 明文只存在于构建环境，不会出现在仓库源码或站点 js 中。
#
# CI（GitHub Actions）不使用本文件，请在仓库
# Settings → Secrets and variables → Actions → New repository secret
# 中新建同名 Secret：ENTERTAINMENT_PASSWORD
ENTERTAINMENT_PASSWORD=
