# 复制本文件为 .env 并填入真实口令（.env 已被 .gitignore 忽略，不会进入仓库）
#
# 娱乐场 /entertainment/ 访问口令。构建时用它加密该目录下页面的正文
# （AES-256-GCM，密钥由口令经 PBKDF2-SHA256 派生），站点产物里只保留密文；
# 浏览器端由 docs/.vuepress/client.ts 中的组件用同一口令解密后渲染。
# 明文只存在于构建环境，不会出现在仓库源码或站点产物中。
#
# 口令请尽量长（建议 16 位以上、非词典词）。密文与 KDF 参数随站点公开，
# 口令强度直接决定抗离线爆破的能力。
#
# CI（GitHub Actions）不使用本文件，请在仓库
# Settings → Secrets and variables → Actions → New repository secret
# 中新建同名 Secret：ENTERTAINMENT_PASSWORD
ENTERTAINMENT_PASSWORD=
